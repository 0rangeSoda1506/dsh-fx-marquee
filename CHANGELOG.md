# 更新日志 · Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的结构，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [0.1.1] — 2026-09-17

走势图新增**X轴单位**与**显示组数**两个可调项。小时档暂时全线下线（灰色不可选）：外汇没有可靠的免密钥日内源，唯一能用的**东方财富** K 线只覆盖 `USD/CNH` 与 `EUR/USD`，最近还被限流了（胆子真是肥嘟嘟的）；为免同一个控件在不同标的上时有时无，其余类别也一并先撤下来测可靠性。

### 新增

- **X 轴单位可选**：自动 / 小时（暂时无法使用） / 天 / 月（点开标的后，浮层内的下拉）
- **数据组数可选**：天 / 月 / 自动档（为 7 的倍数，最少 14 组，最高 56 组）
- 两个选择都会记住，下次打开保持
- 浮层**会标注**当前实际画的是哪一档，例如外汇选「自动」时显示「当前：自动（天）」
- 不可用的档位**灰色**并标注「（无数据源）」，悬停给出原因

### 变更

- **小时档暂停提供**（选项保留可见但不可选）。原因：唯一免密钥的外汇日内源是东方财富 K 线，
  它只覆盖 `USD/CNH` 与 `EUR/USD` 两个品种，且该接口目前对所有主机与编号子域都拒绝请求
  （`UND_ERR_SOCKET`）；普通货币对本身只有 ECB 每日参考价。
  恢复方式：把 `lib/quotes.js` 中的 `HOUR_UNIT_ENABLED` 改为 `true`（这只能让你选到有小时数据的内容）
  —— 取数逻辑、12 步进的组数规则及其测试都完整保留
- 指数的天/月线改走**腾讯** K 线接口（东方财富 `kline` 全线被限流，其行情与分时接口仍正常）；小时档的取数逻辑一并备好，随开关恢复
- 「自动」档位改为跟随组数：外汇与加密货币按各自自然粒度取 N 个点；指数的自动档仍是全天分时线

### 修复

- **单个上游失败会拖垮整组走势数据**：0.1.0 里某个数据源网络一抖，该分组（外汇 / 指数 / 加密）的走势图就会**整块消失**——指数被东财限流时撞上过。现在只影响取不到数据的那个标的，同组其余照常
- 有问题记得交 issue 喵。

### 已知限制

- **小时档**：见上，已暂停
- **外汇小时线没有免密钥来源**：Yahoo 图表接口直连返回 403（需 cookie/crumb，只能靠代理，暂时没有接入，等测试完成后会加入）；
  Stooq 返回 HTML 而非 CSV；其余可用来源（Twelve Data、Alpha Vantage 等）均需 API key
- 小时档的组数规则为 12 的倍数，下限12，上限 60（按半天步进）；切换单位时原组数对新单位无效会自动收敛
  （小时 24，其余 28）

---

## [0.1.0] — 2026-09-17

首个发布版本（npm `dsh-fx-marquee@0.1.0`）。

### 新增

- **行情跑马灯**：输入框上方常驻一条，货币对、A股/指数、加密货币可混排；显示名称、数值、涨跌幅
- 内容放得下时静态居中，放不下才滚动
- **走势图**（点击标的）：Y 轴数值刻度 + 虚线网格、X 轴显示首末时间（按类别自动取样：外汇按日、指数按当日分时、加密按小时）、鼠标悬停吸附取样点
- **交易时段**：按标的所属市场的本地时区显示市场时钟（北京/纽约/UTC）、开市状态、距开盘/收盘倒计时；**休市时曲线转灰**
- **汇率计算器**（正文区上方 Tab）：24 个货币对、汇率取打开时的跑马灯数值并冻结（只有点「刷新汇率」才更新）、结果保留两位小数、支持 `⇅` 调换方向
- **设置页**（设置 → 汇率跑马灯）：一键开关跑马灯（关闭即停止后台轮询）、19 个常用货币对 + 6 个指数/加密标的勾选、自定义添加、**汇率信源可选**（自动 / 新浪财经 / ECB / ER-API）、自动更新间隔（关闭 / 5 秒 / 10 秒 / 20 秒 / 30 秒 / 1 分钟 / 5 分钟）、走势图开关、涨跌配色、实时预览
- 新会话未开始聊天前（Hero 阶段）不显示跑马灯

### 说明

- 全部数据源免密钥；宿主端统一代理并缓存（25 秒），浏览器不直连上游
- Windows / macOS / Linux 通用
